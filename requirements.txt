# Core web framework
fastapi==0.115.6
# ASGI server used to run the FastAPI app
uvicorn[standard]==0.34.0
# Data validation (used for request/response schemas)
pydantic==2.10.4
# Loads environment variables from .env file
python-dotenv==1.0.1
